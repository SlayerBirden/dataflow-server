paths:
  tests: tests
  output: tests/_output
  data: tests/_data
  support: tests/_support

settings:
  shuffle: false
  lint: true

coverage:
  enabled: true
  include:
    - src/*
  remote_context_options:
    http:
      timeout: 60
    ssl:
      verify_peer: false
