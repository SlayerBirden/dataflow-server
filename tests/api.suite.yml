actor: ApiTester
modules:
  enabled:
    - REST:
        depends: PhpBrowser
    - CleanDoctrine2:
        depends: ZendExpressive3
        cleanup: true
    - \Helper\Api
    - Asserts
  config:
    REST:
      url: https://dataflow.test
